html
    head
        meta(charset='utf-8')
    body
        h2 
            a(href='/topic') Server Side JavaScript
        ul
            each topic in topics
                li
                    a(href='/topic/'+topic)=topic
        article
            h2=title
            =description
        a(href='/topic/new') New        