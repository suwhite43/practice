html
    head
        meta(charset='utf-8')
    body
        h2 
            a(href='/topic') Server Side JavaScript
        ul
            each topic in topics
                li
                    a(href='/topic/'+topic.id)=topic.title
        article
            if subtopic
                h2=subtopic.title
                =subtopic.description
            else
                h2 Welcome
                | This is Serverside Javascript tutorial.
        a(href='/topic/new') New        